<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DesktopSnap.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DesktopSnap"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" BorderBrush="#333333" BorderThickness="0,0,1,0" Padding="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="{x:Bind Lang.AppTitle, Mode=OneWay}" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,10"/>
            
            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10" Margin="0,0,0,20">
                <TextBlock Text="{x:Bind Lang.Language, Mode=OneWay}" VerticalAlignment="Center" Foreground="Gray" FontSize="12" />
                <ComboBox x:Name="LangCombo" SelectionChanged="LangCombo_SelectionChanged" FontSize="12" MinWidth="100">
                    <ComboBoxItem Content="Auto" Tag="auto" />
                    <ComboBoxItem Content="中文" Tag="zh" />
                    <ComboBoxItem Content="English" Tag="en" />
                </ComboBox>
            </StackPanel>

            <Button Grid.Row="2" Content="{x:Bind Lang.NewSnapshot, Mode=OneWay}" HorizontalAlignment="Stretch" Click="NewLayoutBtn_Click" Margin="0,0,0,10" Style="{StaticResource AccentButtonStyle}"/>

            <ListView Grid.Row="3" x:Name="LayoutsListView" SelectionChanged="LayoutsListView_SelectionChanged" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,8">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" />
                            <TextBlock Text="{Binding SavedTime}" FontSize="12" Foreground="DarkGray" Margin="0,4,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Grid Grid.Column="1" Padding="20,0,0,0">
            <Grid x:Name="DetailPanel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,15">
                    <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal" Spacing="4" x:Name="RenamePanel" Visibility="Collapsed">
                            <TextBox x:Name="NameEditBox" Width="200" VerticalAlignment="Center" KeyDown="NameEditBox_KeyDown" LostFocus="NameEditBox_LostFocus" />
                            <Button x:Name="RenameConfirmBtn" Background="Transparent" BorderThickness="0" Padding="4" VerticalAlignment="Center" Click="RenameConfirmBtn_Click">
                                <FontIcon Glyph="&#xE73E;" FontSize="14" Foreground="LightGreen" />
                            </Button>
                            <Button x:Name="RenameCancelBtn" Background="Transparent" BorderThickness="0" Padding="4" VerticalAlignment="Center" Click="RenameCancelBtn_Click">
                                <FontIcon Glyph="&#xE711;" FontSize="14" Foreground="IndianRed" />
                            </Button>
                        </StackPanel>
                        <TextBlock x:Name="DetailNameText" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" />
                        <Button x:Name="RenameBtn" Background="Transparent" BorderThickness="0" Padding="4" VerticalAlignment="Center" Click="RenameBtn_Click" ToolTipService.ToolTip="{x:Bind Lang.Rename, Mode=OneWay}">
                            <FontIcon Glyph="&#xE70F;" FontSize="14" />
                        </Button>
                        <TextBlock x:Name="DetailCountText" VerticalAlignment="Center" Foreground="Gray" FontSize="14" Margin="10,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="5" x:Name="DesktopJumpsPanel">
                            <!-- Buttons will be dynamically generated here -->
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <TextBlock Text="{x:Bind Lang.Zoom, Mode=OneWay}" VerticalAlignment="Center" Foreground="Gray" Margin="0,0,5,0" />
                            <Button Content="{x:Bind Lang.FitScreen, Mode=OneWay}" Click="ZoomFitBtn_Click" Padding="8,4" />
                            <Button Content="100%" Click="ZoomOriginalBtn_Click" Padding="8,4" />
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <Border Grid.Row="1" Background="#111111" CornerRadius="8" BorderBrush="#333333" BorderThickness="1" Margin="0,0,0,20">
                    <ScrollViewer x:Name="PreviewScrollViewer" 
                                  ZoomMode="Enabled" 
                                  HorizontalScrollBarVisibility="Auto" 
                                  VerticalScrollBarVisibility="Auto" 
                                  HorizontalScrollMode="Enabled" 
                                  VerticalScrollMode="Enabled">
                        <Canvas x:Name="PreviewCanvas" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    </ScrollViewer>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="10" HorizontalAlignment="Right" Margin="0,0,0,20">
                    <Button x:Name="OverwriteBtn" Content="{x:Bind Lang.Overwrite, Mode=OneWay}" Click="OverwriteBtn_Click" />
                    <Button x:Name="DeleteSnapshotBtn" Content="{x:Bind Lang.Delete, Mode=OneWay}" Click="DeleteBtn_Click" />
                    <Button x:Name="RestoreSnapshotBtn" Content="{x:Bind Lang.Restore, Mode=OneWay}" Click="RestoreBtn_Click" Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>

                <InfoBar Grid.Row="3" x:Name="StatusInfo" IsOpen="False" />
            </Grid>
            <TextBlock x:Name="NoSelectionText" Text="{x:Bind Lang.NoSelection, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Gray" FontSize="16" />
            
            <!-- Loading Overlay for Restore -->
            <Grid x:Name="LoadingOverlay" Background="#99000000" Visibility="Collapsed" Canvas.ZIndex="100">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="16">
                    <ProgressRing IsActive="True" Width="64" Height="64" />
                    <TextBlock Text="{x:Bind Lang.RestoringWait, Mode=OneWay}" FontSize="16" FontWeight="SemiBold" Foreground="White" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
